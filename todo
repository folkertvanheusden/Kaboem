- auto adjust tempo by what is entered
- swirl channel through left/right -> 'autopan'
- shorten filenames (with '...') when they don't fit on screen
- adjust fontsizes so that text fits
- %zu / %zd -> size_t / ssize_t for uint64_t / int64_t verify
- skips a beat after un-pause ==> caused by not unpausing at a precise interval?
- external sync (via midi?)

- synquencer
  - name is not stored correctly
  - set volume for midi input
- support for multiple patterns, switchable at "runtime" via midi messages
